<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ModelBuddy.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:ModelBuddy.Views"
    xmlns:models="using:ModelBuddy.Models"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <FontIcon Glyph="&#xE7BA;" FontSize="28"/>
            <TextBlock Text="Logs" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            <TextBlock Text="{x:Bind ViewModel.TotalLogCount, Mode=OneWay}" 
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"/>
            <TextBlock Text="entries" 
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBox 
                    x:Name="SearchBox"
                    PlaceholderText="Search logs..."
                    Text="{x:Bind ViewModel.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Width="300"/>
                <ComboBox 
                    x:Name="LogLevelFilter"
                    Header="Minimum Level"
                    ItemsSource="{x:Bind ViewModel.LogLevels}"
                    SelectedItem="{x:Bind ViewModel.SelectedLogLevel, Mode=TwoWay}"
                    Width="150"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Command="{x:Bind ViewModel.RefreshLogsCommand}" ToolTipService.ToolTip="Refresh">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                </Button>
                <Button Command="{x:Bind ViewModel.ClearLogsCommand}" ToolTipService.ToolTip="Clear Logs">
                    <FontIcon Glyph="&#xE74D;" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Logs List -->
        <ListView 
            x:Name="LogsList"
            Grid.Row="2"
            ItemsSource="{x:Bind ViewModel.Logs, Mode=OneWay}"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LogEntry">
                    <Grid Padding="8,12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock 
                            Grid.Column="0" 
                            Text="{x:Bind local:LogsPage.FormatTimestamp(Timestamp)}" 
                            Style="{StaticResource CaptionTextBlockStyle}" 
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                            VerticalAlignment="Center"/>
                        <Border 
                            Grid.Column="1" 
                            CornerRadius="4" 
                            Padding="6,2" 
                            HorizontalAlignment="Left" 
                            Background="{x:Bind local:LogsPage.GetLevelBrush(Level)}">
                            <TextBlock Text="{x:Bind Level}" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                        </Border>
                        <TextBlock 
                            Grid.Column="2" 
                            Text="{x:Bind Source}" 
                            Style="{StaticResource CaptionTextBlockStyle}" 
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                            TextTrimming="CharacterEllipsis" 
                            VerticalAlignment="Center"/>
                        <TextBlock 
                            Grid.Column="3" 
                            Text="{x:Bind Message}" 
                            TextWrapping="NoWrap" 
                            TextTrimming="CharacterEllipsis" 
                            VerticalAlignment="Center" 
                            ToolTipService.ToolTip="{x:Bind Message}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Empty state -->
        <StackPanel 
            Grid.Row="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12"
            Visibility="{x:Bind local:LogsPage.IsEmpty(ViewModel.Logs.Count), Mode=OneWay}">
            <FontIcon Glyph="&#xE7BA;" FontSize="48" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock 
                Text="No logs to display" 
                Style="{StaticResource SubtitleTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBlock 
                Text="Logs will appear here as the application runs."
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
        </StackPanel>

        <!-- Status Bar -->
        <Border 
            Grid.Row="3" 
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="4"
            Padding="12,8">
            <TextBlock 
                Text="{x:Bind local:LogsPage.FormatStatus(ViewModel.Logs.Count, ViewModel.TotalLogCount), Mode=OneWay}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </Border>
    </Grid>
</Page>
